import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class IpcService {

  constructor() {

    // const port = chrome.runtime.connect({ name: 'Colosseum' });
    // chrome.tabs.connect()
  }
}
